const express = require('express');
const PORT = process.env.PORT || 3001;
const knex = require('./knex/knex.js');
const app = express();

app.get('/tasks', (req, res) => {
  // use the knex variable above to create dynamic queries
});

app.listen(PORT, () => {
  console.log(`Listening on port: ${PORT}`);
});

import express from "express";
import bodyParser from "body-parser";
import {
    initializeDatabase,
    readTeachers,
    readTeacherInfo,
    addTeacher,
    deleteTeacher,
} from "./database.js";

// Importing necessary modulesapp.post("/getTeacherInfo", async function (req, res) {
    // Implementation to retrieve a single teacher}
;

import express from "express";
import bodyParser from "body-parser";
import {
    initializeDatabase,
    readTeachers,
    readTeacherInfo,
    addTeacher,
    deleteTeacher,
} from "./database.js";

// Create an Express application instance
constapp= express();

// Use body-parser middleware
app.use(bodyParser.urlencoded({ extended: false }));
app.use(bodyParser.json());

// ... Define your APIs and further code ...

// Export the Express app
module.exports = app;
app.get("/listTeachers", async function (req, res) {
    // Implementation to retrieve teachers
});// ... Previous imports and code ...

// API for retrieving a single teacher
app.post("/getTeacherInfo", async function (req, res) {
    let reqBody = req.body;
    console.log("Request received to get Teacher Info");
    let data = await readTeacherInfo(reqBody.id);

    res.setHeader("Content-Type", "application/json");
    res.end(JSON.stringify(data));
});

// ... Rest of your code ...

// Export the Express app
module.exports = app;app.post("/addTeacher", async function (req, res) {
    // Implementation to add a teacher
});
app.post("/deleteTeacher", async function (req, res) {
    // Implementation to delete a teacher
});
module.exports = app;


